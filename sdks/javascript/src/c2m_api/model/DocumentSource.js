/**
 * C2M API v2 - Auth Overlay
 * API for submitting documents with various routing options
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RequestIdSource from './RequestIdSource';
import ZipDocumentIdSource from './ZipDocumentIdSource';
import ZipRequestIdSource from './ZipRequestIdSource';

/**
 * The DocumentSource model module.
 * @module c2m_api/model/DocumentSource
 * @version 2.0.0
 */
class DocumentSource {
    /**
     * Constructs a new <code>DocumentSource</code>.
     * @alias module:c2m_api/model/DocumentSource
     * @param {(module:c2m_api/model/Number|module:c2m_api/model/RequestIdSource|module:c2m_api/model/String|module:c2m_api/model/ZipDocumentIdSource|module:c2m_api/model/ZipRequestIdSource)} instance The actual instance to initialize DocumentSource.
     */
    constructor(instance = null) {
        if (instance === null) {
            this.actualInstance = null;
            return;
        }
        var match = 0;
        var errorMessages = [];
        try {
            // validate integer
            if (!(typeof instance === 'number' && instance % 1 === 0)) {
                throw new Error("Invalid value. Must be integer. Input: " + JSON.stringify(instance));
            }
            this.actualInstance = instance;
            match++;
        } catch(err) {
            // json data failed to deserialize into Number
            errorMessages.push("Failed to construct Number: " + err)
        }

        try {
            if (typeof instance === "RequestIdSource") {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                RequestIdSource.validateJSON(instance); // throw an exception if no match
                // create RequestIdSource from JS object
                this.actualInstance = RequestIdSource.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into RequestIdSource
            errorMessages.push("Failed to construct RequestIdSource: " + err)
        }

        try {
            // validate string
            if (!(typeof instance === 'string')) {
                throw new Error("Invalid value. Must be string. Input: " + JSON.stringify(instance));
            }
            this.actualInstance = instance;
            match++;
        } catch(err) {
            // json data failed to deserialize into String
            errorMessages.push("Failed to construct String: " + err)
        }

        try {
            if (typeof instance === "ZipDocumentIdSource") {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                ZipDocumentIdSource.validateJSON(instance); // throw an exception if no match
                // create ZipDocumentIdSource from JS object
                this.actualInstance = ZipDocumentIdSource.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into ZipDocumentIdSource
            errorMessages.push("Failed to construct ZipDocumentIdSource: " + err)
        }

        try {
            if (typeof instance === "ZipRequestIdSource") {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                ZipRequestIdSource.validateJSON(instance); // throw an exception if no match
                // create ZipRequestIdSource from JS object
                this.actualInstance = ZipRequestIdSource.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into ZipRequestIdSource
            errorMessages.push("Failed to construct ZipRequestIdSource: " + err)
        }

        if (match > 1) {
            throw new Error("Multiple matches found constructing `DocumentSource` with oneOf schemas Number, RequestIdSource, String, ZipDocumentIdSource, ZipRequestIdSource. Input: " + JSON.stringify(instance));
        } else if (match === 0) {
            this.actualInstance = null; // clear the actual instance in case there are multiple matches
            throw new Error("No match found constructing `DocumentSource` with oneOf schemas Number, RequestIdSource, String, ZipDocumentIdSource, ZipRequestIdSource. Details: " +
                            errorMessages.join(", "));
        } else { // only 1 match
            // the input is valid
        }
    }

    /**
     * Constructs a <code>DocumentSource</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:c2m_api/model/DocumentSource} obj Optional instance to populate.
     * @return {module:c2m_api/model/DocumentSource} The populated <code>DocumentSource</code> instance.
     */
    static constructFromObject(data, obj) {
        return new DocumentSource(data);
    }

    /**
     * Gets the actual instance, which can be <code>Number</code>, <code>RequestIdSource</code>, <code>String</code>, <code>ZipDocumentIdSource</code>, <code>ZipRequestIdSource</code>.
     * @return {(module:c2m_api/model/Number|module:c2m_api/model/RequestIdSource|module:c2m_api/model/String|module:c2m_api/model/ZipDocumentIdSource|module:c2m_api/model/ZipRequestIdSource)} The actual instance.
     */
    getActualInstance() {
        return this.actualInstance;
    }

    /**
     * Sets the actual instance, which can be <code>Number</code>, <code>RequestIdSource</code>, <code>String</code>, <code>ZipDocumentIdSource</code>, <code>ZipRequestIdSource</code>.
     * @param {(module:c2m_api/model/Number|module:c2m_api/model/RequestIdSource|module:c2m_api/model/String|module:c2m_api/model/ZipDocumentIdSource|module:c2m_api/model/ZipRequestIdSource)} obj The actual instance.
     */
    setActualInstance(obj) {
       this.actualInstance = DocumentSource.constructFromObject(obj).getActualInstance();
    }

    /**
     * Returns the JSON representation of the actual instance.
     * @return {string}
     */
    toJSON = function(){
        return this.getActualInstance();
    }

    /**
     * Create an instance of DocumentSource from a JSON string.
     * @param {string} json_string JSON string.
     * @return {module:c2m_api/model/DocumentSource} An instance of DocumentSource.
     */
    static fromJSON = function(json_string){
        return DocumentSource.constructFromObject(JSON.parse(json_string));
    }
}

/**
 * @member {Number} requestId
 */
DocumentSource.prototype['requestId'] = undefined;

/**
 * @member {String} filename
 */
DocumentSource.prototype['filename'] = undefined;

/**
 * @member {Number} zipDocumentId
 */
DocumentSource.prototype['zipDocumentId'] = undefined;

/**
 * @member {String} zipFilename
 */
DocumentSource.prototype['zipFilename'] = undefined;


DocumentSource.OneOf = ["Number", "RequestIdSource", "String", "ZipDocumentIdSource", "ZipRequestIdSource"];

export default DocumentSource;

