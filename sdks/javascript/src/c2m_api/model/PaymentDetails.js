/**
 * C2M API v2
 * API for submitting mailing jobs with various document routing options
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AchDetails from './AchDetails';
import AchPayment from './AchPayment';
import CreditAmount from './CreditAmount';
import CreditCardDetails from './CreditCardDetails';
import CreditCardPayment from './CreditCardPayment';
import InvoiceDetails from './InvoiceDetails';
import InvoicePayment from './InvoicePayment';
import UserCreditPayment from './UserCreditPayment';

/**
 * The PaymentDetails model module.
 * @module c2m_api/model/PaymentDetails
 * @version 2.0.0
 */
class PaymentDetails {
    /**
     * Constructs a new <code>PaymentDetails</code>.
     * @alias module:c2m_api/model/PaymentDetails
     * @param {(module:c2m_api/model/AchPayment|module:c2m_api/model/CreditCardPayment|module:c2m_api/model/InvoicePayment|module:c2m_api/model/UserCreditPayment)} instance The actual instance to initialize PaymentDetails.
     */
    constructor(instance = null) {
        if (instance === null) {
            this.actualInstance = null;
            return;
        }
        var match = 0;
        var errorMessages = [];
        try {
            if (typeof instance === "CreditCardPayment") {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                CreditCardPayment.validateJSON(instance); // throw an exception if no match
                // create CreditCardPayment from JS object
                this.actualInstance = CreditCardPayment.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into CreditCardPayment
            errorMessages.push("Failed to construct CreditCardPayment: " + err)
        }

        try {
            if (typeof instance === "InvoicePayment") {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                InvoicePayment.validateJSON(instance); // throw an exception if no match
                // create InvoicePayment from JS object
                this.actualInstance = InvoicePayment.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into InvoicePayment
            errorMessages.push("Failed to construct InvoicePayment: " + err)
        }

        try {
            if (typeof instance === "AchPayment") {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                AchPayment.validateJSON(instance); // throw an exception if no match
                // create AchPayment from JS object
                this.actualInstance = AchPayment.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into AchPayment
            errorMessages.push("Failed to construct AchPayment: " + err)
        }

        try {
            if (typeof instance === "UserCreditPayment") {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                UserCreditPayment.validateJSON(instance); // throw an exception if no match
                // create UserCreditPayment from JS object
                this.actualInstance = UserCreditPayment.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into UserCreditPayment
            errorMessages.push("Failed to construct UserCreditPayment: " + err)
        }

        if (match > 1) {
            throw new Error("Multiple matches found constructing `PaymentDetails` with oneOf schemas AchPayment, CreditCardPayment, InvoicePayment, UserCreditPayment. Input: " + JSON.stringify(instance));
        } else if (match === 0) {
            this.actualInstance = null; // clear the actual instance in case there are multiple matches
            throw new Error("No match found constructing `PaymentDetails` with oneOf schemas AchPayment, CreditCardPayment, InvoicePayment, UserCreditPayment. Details: " +
                            errorMessages.join(", "));
        } else { // only 1 match
            // the input is valid
        }
    }

    /**
     * Constructs a <code>PaymentDetails</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:c2m_api/model/PaymentDetails} obj Optional instance to populate.
     * @return {module:c2m_api/model/PaymentDetails} The populated <code>PaymentDetails</code> instance.
     */
    static constructFromObject(data, obj) {
        return new PaymentDetails(data);
    }

    /**
     * Gets the actual instance, which can be <code>AchPayment</code>, <code>CreditCardPayment</code>, <code>InvoicePayment</code>, <code>UserCreditPayment</code>.
     * @return {(module:c2m_api/model/AchPayment|module:c2m_api/model/CreditCardPayment|module:c2m_api/model/InvoicePayment|module:c2m_api/model/UserCreditPayment)} The actual instance.
     */
    getActualInstance() {
        return this.actualInstance;
    }

    /**
     * Sets the actual instance, which can be <code>AchPayment</code>, <code>CreditCardPayment</code>, <code>InvoicePayment</code>, <code>UserCreditPayment</code>.
     * @param {(module:c2m_api/model/AchPayment|module:c2m_api/model/CreditCardPayment|module:c2m_api/model/InvoicePayment|module:c2m_api/model/UserCreditPayment)} obj The actual instance.
     */
    setActualInstance(obj) {
       this.actualInstance = PaymentDetails.constructFromObject(obj).getActualInstance();
    }

    /**
     * Returns the JSON representation of the actual instance.
     * @return {string}
     */
    toJSON = function(){
        return this.getActualInstance();
    }

    /**
     * Create an instance of PaymentDetails from a JSON string.
     * @param {string} json_string JSON string.
     * @return {module:c2m_api/model/PaymentDetails} An instance of PaymentDetails.
     */
    static fromJSON = function(json_string){
        return PaymentDetails.constructFromObject(JSON.parse(json_string));
    }
}

/**
 * @member {module:c2m_api/model/CreditCardDetails} creditCardDetails
 */
PaymentDetails.prototype['creditCardDetails'] = undefined;

/**
 * @member {module:c2m_api/model/InvoiceDetails} invoiceDetails
 */
PaymentDetails.prototype['invoiceDetails'] = undefined;

/**
 * @member {module:c2m_api/model/AchDetails} achDetails
 */
PaymentDetails.prototype['achDetails'] = undefined;

/**
 * @member {module:c2m_api/model/CreditAmount} creditAmount
 */
PaymentDetails.prototype['creditAmount'] = undefined;


PaymentDetails.OneOf = ["AchPayment", "CreditCardPayment", "InvoicePayment", "UserCreditPayment"];

export default PaymentDetails;

