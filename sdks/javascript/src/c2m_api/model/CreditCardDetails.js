/**
 * C2M API v2 - Auth Overlay
 * API for submitting documents with various routing options
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ExpirationDate from './ExpirationDate';

/**
 * The CreditCardDetails model module.
 * @module c2m_api/model/CreditCardDetails
 * @version 2.0.0
 */
class CreditCardDetails {
    /**
     * Constructs a new <code>CreditCardDetails</code>.
     * @alias module:c2m_api/model/CreditCardDetails
     * @param cardType {module:c2m_api/model/CreditCardDetails.CardTypeEnum} 
     * @param cardNumber {String} 
     * @param expirationDate {module:c2m_api/model/ExpirationDate} 
     * @param cvv {Number} 
     */
    constructor(cardType, cardNumber, expirationDate, cvv) { 
        
        CreditCardDetails.initialize(this, cardType, cardNumber, expirationDate, cvv);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, cardType, cardNumber, expirationDate, cvv) { 
        obj['cardType'] = cardType;
        obj['cardNumber'] = cardNumber;
        obj['expirationDate'] = expirationDate;
        obj['cvv'] = cvv;
    }

    /**
     * Constructs a <code>CreditCardDetails</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:c2m_api/model/CreditCardDetails} obj Optional instance to populate.
     * @return {module:c2m_api/model/CreditCardDetails} The populated <code>CreditCardDetails</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreditCardDetails();

            if (data.hasOwnProperty('cardType')) {
                obj['cardType'] = ApiClient.convertToType(data['cardType'], 'String');
            }
            if (data.hasOwnProperty('cardNumber')) {
                obj['cardNumber'] = ApiClient.convertToType(data['cardNumber'], 'String');
            }
            if (data.hasOwnProperty('expirationDate')) {
                obj['expirationDate'] = ExpirationDate.constructFromObject(data['expirationDate']);
            }
            if (data.hasOwnProperty('cvv')) {
                obj['cvv'] = ApiClient.convertToType(data['cvv'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreditCardDetails</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreditCardDetails</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreditCardDetails.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['cardType'] && !(typeof data['cardType'] === 'string' || data['cardType'] instanceof String)) {
            throw new Error("Expected the field `cardType` to be a primitive type in the JSON string but got " + data['cardType']);
        }
        // ensure the json data is a string
        if (data['cardNumber'] && !(typeof data['cardNumber'] === 'string' || data['cardNumber'] instanceof String)) {
            throw new Error("Expected the field `cardNumber` to be a primitive type in the JSON string but got " + data['cardNumber']);
        }
        // validate the optional field `expirationDate`
        if (data['expirationDate']) { // data not null
          ExpirationDate.validateJSON(data['expirationDate']);
        }

        return true;
    }


}

CreditCardDetails.RequiredProperties = ["cardType", "cardNumber", "expirationDate", "cvv"];

/**
 * @member {module:c2m_api/model/CreditCardDetails.CardTypeEnum} cardType
 */
CreditCardDetails.prototype['cardType'] = undefined;

/**
 * @member {String} cardNumber
 */
CreditCardDetails.prototype['cardNumber'] = undefined;

/**
 * @member {module:c2m_api/model/ExpirationDate} expirationDate
 */
CreditCardDetails.prototype['expirationDate'] = undefined;

/**
 * @member {Number} cvv
 */
CreditCardDetails.prototype['cvv'] = undefined;





/**
 * Allowed values for the <code>cardType</code> property.
 * @enum {String}
 * @readonly
 */
CreditCardDetails['CardTypeEnum'] = {

    /**
     * value: "visa"
     * @const
     */
    "visa": "visa",

    /**
     * value: "mastercard"
     * @const
     */
    "mastercard": "mastercard",

    /**
     * value: "discover"
     * @const
     */
    "discover": "discover",

    /**
     * value: "americanExpress"
     * @const
     */
    "americanExpress": "americanExpress"
};



export default CreditCardDetails;

