/**
 * C2M API v2
 * API for submitting mailing jobs with various document routing options
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The LongTokenResponse model module.
 * @module c2m_api/model/LongTokenResponse
 * @version 2.0.0
 */
class LongTokenResponse {
    /**
     * Constructs a new <code>LongTokenResponse</code>.
     * @alias module:c2m_api/model/LongTokenResponse
     * @param tokenType {module:c2m_api/model/LongTokenResponse.TokenTypeEnum} 
     * @param accessToken {String} Long-lived token (opaque or JWT depending on deployment)
     * @param expiresIn {Number} Lifetime in seconds
     * @param expiresAt {Date} ISO 8601 timestamp of expiration
     */
    constructor(tokenType, accessToken, expiresIn, expiresAt) { 
        
        LongTokenResponse.initialize(this, tokenType, accessToken, expiresIn, expiresAt);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, tokenType, accessToken, expiresIn, expiresAt) { 
        obj['token_type'] = tokenType;
        obj['access_token'] = accessToken;
        obj['expires_in'] = expiresIn;
        obj['expires_at'] = expiresAt;
    }

    /**
     * Constructs a <code>LongTokenResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:c2m_api/model/LongTokenResponse} obj Optional instance to populate.
     * @return {module:c2m_api/model/LongTokenResponse} The populated <code>LongTokenResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LongTokenResponse();

            if (data.hasOwnProperty('token_type')) {
                obj['token_type'] = ApiClient.convertToType(data['token_type'], 'String');
            }
            if (data.hasOwnProperty('access_token')) {
                obj['access_token'] = ApiClient.convertToType(data['access_token'], 'String');
            }
            if (data.hasOwnProperty('expires_in')) {
                obj['expires_in'] = ApiClient.convertToType(data['expires_in'], 'Number');
            }
            if (data.hasOwnProperty('expires_at')) {
                obj['expires_at'] = ApiClient.convertToType(data['expires_at'], 'Date');
            }
            if (data.hasOwnProperty('scopes')) {
                obj['scopes'] = ApiClient.convertToType(data['scopes'], ['String']);
            }
            if (data.hasOwnProperty('token_id')) {
                obj['token_id'] = ApiClient.convertToType(data['token_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LongTokenResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LongTokenResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of LongTokenResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['token_type'] && !(typeof data['token_type'] === 'string' || data['token_type'] instanceof String)) {
            throw new Error("Expected the field `token_type` to be a primitive type in the JSON string but got " + data['token_type']);
        }
        // ensure the json data is a string
        if (data['access_token'] && !(typeof data['access_token'] === 'string' || data['access_token'] instanceof String)) {
            throw new Error("Expected the field `access_token` to be a primitive type in the JSON string but got " + data['access_token']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['scopes'])) {
            throw new Error("Expected the field `scopes` to be an array in the JSON data but got " + data['scopes']);
        }
        // ensure the json data is a string
        if (data['token_id'] && !(typeof data['token_id'] === 'string' || data['token_id'] instanceof String)) {
            throw new Error("Expected the field `token_id` to be a primitive type in the JSON string but got " + data['token_id']);
        }

        return true;
    }


}

LongTokenResponse.RequiredProperties = ["token_type", "access_token", "expires_in", "expires_at"];

/**
 * @member {module:c2m_api/model/LongTokenResponse.TokenTypeEnum} token_type
 */
LongTokenResponse.prototype['token_type'] = undefined;

/**
 * Long-lived token (opaque or JWT depending on deployment)
 * @member {String} access_token
 */
LongTokenResponse.prototype['access_token'] = undefined;

/**
 * Lifetime in seconds
 * @member {Number} expires_in
 */
LongTokenResponse.prototype['expires_in'] = undefined;

/**
 * ISO 8601 timestamp of expiration
 * @member {Date} expires_at
 */
LongTokenResponse.prototype['expires_at'] = undefined;

/**
 * Granted scopes
 * @member {Array.<String>} scopes
 */
LongTokenResponse.prototype['scopes'] = undefined;

/**
 * Server-issued identifier for this token
 * @member {String} token_id
 */
LongTokenResponse.prototype['token_id'] = undefined;





/**
 * Allowed values for the <code>token_type</code> property.
 * @enum {String}
 * @readonly
 */
LongTokenResponse['TokenTypeEnum'] = {

    /**
     * value: "Bearer"
     * @const
     */
    "Bearer": "Bearer"
};



export default LongTokenResponse;

