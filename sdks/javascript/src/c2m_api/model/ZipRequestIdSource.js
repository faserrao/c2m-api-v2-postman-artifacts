/**
 * C2M API v2 - Auth Overlay
 * API for submitting documents with various routing options
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ZipRequestIdSource model module.
 * @module c2m_api/model/ZipRequestIdSource
 * @version 2.0.0
 */
class ZipRequestIdSource {
    /**
     * Constructs a new <code>ZipRequestIdSource</code>.
     * @alias module:c2m_api/model/ZipRequestIdSource
     * @param requestId {Number} 
     * @param zipFilename {String} 
     * @param filename {String} 
     */
    constructor(requestId, zipFilename, filename) { 
        
        ZipRequestIdSource.initialize(this, requestId, zipFilename, filename);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, requestId, zipFilename, filename) { 
        obj['requestId'] = requestId;
        obj['zipFilename'] = zipFilename;
        obj['filename'] = filename;
    }

    /**
     * Constructs a <code>ZipRequestIdSource</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:c2m_api/model/ZipRequestIdSource} obj Optional instance to populate.
     * @return {module:c2m_api/model/ZipRequestIdSource} The populated <code>ZipRequestIdSource</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ZipRequestIdSource();

            if (data.hasOwnProperty('requestId')) {
                obj['requestId'] = ApiClient.convertToType(data['requestId'], 'Number');
            }
            if (data.hasOwnProperty('zipFilename')) {
                obj['zipFilename'] = ApiClient.convertToType(data['zipFilename'], 'String');
            }
            if (data.hasOwnProperty('filename')) {
                obj['filename'] = ApiClient.convertToType(data['filename'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ZipRequestIdSource</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ZipRequestIdSource</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ZipRequestIdSource.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['zipFilename'] && !(typeof data['zipFilename'] === 'string' || data['zipFilename'] instanceof String)) {
            throw new Error("Expected the field `zipFilename` to be a primitive type in the JSON string but got " + data['zipFilename']);
        }
        // ensure the json data is a string
        if (data['filename'] && !(typeof data['filename'] === 'string' || data['filename'] instanceof String)) {
            throw new Error("Expected the field `filename` to be a primitive type in the JSON string but got " + data['filename']);
        }

        return true;
    }


}

ZipRequestIdSource.RequiredProperties = ["requestId", "zipFilename", "filename"];

/**
 * @member {Number} requestId
 */
ZipRequestIdSource.prototype['requestId'] = undefined;

/**
 * @member {String} zipFilename
 */
ZipRequestIdSource.prototype['zipFilename'] = undefined;

/**
 * @member {String} filename
 */
ZipRequestIdSource.prototype['filename'] = undefined;






export default ZipRequestIdSource;

